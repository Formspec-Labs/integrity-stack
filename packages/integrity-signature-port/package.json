{
  "name": "@integrity-stack/signature-port",
  "version": "0.1.0",
  "private": true,
  "description": "Generic signature verifier port and verification receipt types",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
